<?php include 'app/templates/menu.phtml'; ?>

<h2><?php echo $data->customer_group_id ? 'Update Customer Group' : 'Create Customer Group'; ?></h2>
<p style="margin-top:0; color:#555;">Maintain group details and status here.</p>

<form method="POST" action="index.php?c=customergroup&a=save">
    <input type="hidden" name="customergroup[customer_group_id]" value="<?php echo $data->customer_group_id; ?>">

    <table border="1" width="100%" cellpadding="6">
        <tr>
            <td>Group Name</td>
            <td><input type="text" name="customergroup[group_name]" value="<?php echo $data->group_name; ?>" placeholder="Wholesale"></td>
        </tr>
        <tr>
            <td>Description</td>
            <td><input type="text" name="customergroup[description]" value="<?php echo $data->description; ?>" placeholder="Group description"></td>
        </tr>
        <tr>
            <td>Status</td>
            <td>
                <select name="customergroup[status]">
                    <option value="1" <?php echo ($data->status == 1) ? 'selected' : ''; ?>>Enabled</option>
                    <option value="0" <?php echo ($data->status == 0) ? 'selected' : ''; ?>>Disabled</option>
                </select>
            </td>
        </tr>
        <tr style="background:#f7f7f7;">
            <td colspan="2">
                <button type="submit">Save Group</button>
                <a href="index.php?c=customergroup&a=list">Back</a>
            </td>
        </tr>
    </table>
</form>
