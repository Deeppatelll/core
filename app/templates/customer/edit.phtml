<?php include 'app/templates/menu.phtml'; ?>

<h2><?php echo $data->customer_id ? 'Update Customer' : 'Create Customer'; ?></h2>
<p style="margin-top:0; color:#555;">Use the form below to manage customer details.</p>

<form method="POST" action="index.php?c=customer&a=save">
    <input type="hidden" name="customer[customer_id]" value="<?php echo $data->customer_id; ?>">

    <table border="1" width="100%" cellpadding="6">
        <tr>
            <td>Customer Group ID</td>
            <td><input type="text" name="customer[customer_group_id]" value="<?php echo $data->customer_group_id; ?>" placeholder="e.g. 1"></td>
        </tr>
        <tr>
            <td>First Name</td>
            <td><input type="text" name="customer[first_name]" value="<?php echo $data->first_name; ?>" placeholder="First name"></td>
        </tr>
        <tr>
            <td>Last Name</td>
            <td><input type="text" name="customer[last_name]" value="<?php echo $data->last_name; ?>" placeholder="Last name"></td>
        </tr>
        <tr>
            <td>Email</td>
            <td><input type="email" name="customer[email]" value="<?php echo $data->email; ?>"></td>
        </tr>
        <tr>
            <td>Phone</td>
            <td><input type="text" name="customer[phone]" value="<?php echo $data->phone; ?>"></td>
        </tr>
        <tr>
            <td>Status</td>
            <td>
                <select name="customer[status]">
                    <option value="1" <?php echo ($data->status == 1) ? 'selected' : ''; ?>>Enabled</option>
                    <option value="0" <?php echo ($data->status == 0) ? 'selected' : ''; ?>>Disabled</option>
                </select>
            </td>
        </tr>
        <tr style="background:#f7f7f7;">
            <td colspan="2">
                <button type="submit">Save Customer</button>
                <a href="index.php?c=customer&a=list">Back</a>
            </td>
        </tr>
    </table>
</form>
